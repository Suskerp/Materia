export { heroShellStyles as styles, SHELL_PAIR } from "./shell.js";

/* The hero's stylesheet IS the shared shell — see shell.js for why the three
 * hero cards are one anatomy rather than three lookalikes. This module stays
 * as the import path every hero card already uses. */
